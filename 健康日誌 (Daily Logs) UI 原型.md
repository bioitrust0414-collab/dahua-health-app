# 健康日誌 (Daily Logs) UI 原型

**對應資料庫表**: `daily_logs`

## 頁面佈局與組件

### 1. 日誌列表/概覽頁面
- **頂部導航**: 顯示當前日期，左右箭頭切換日期，或點擊日期打開日曆選擇器。
- **數據概覽卡片**: 顯示當日或選定時間範圍內的總結數據（例如：總飲水量、平均睡眠時長）。
- **日誌條目列表**: 
    - 每個條目顯示 `log_date`、`water_ml`、`sleep_hours`、`notes`。
    - 可點擊條目進入編輯模式或查看詳情。
    - 使用 `shadcn/ui` 的 `Card` 和 `Table` 組件展示。
- **新增日誌按鈕**: 浮動在頁面右下角，點擊後彈出新增/編輯日誌表單。

### 2. 新增/編輯日誌表單 (彈窗或新頁面)
- **日期選擇器**: 預設為當前日期，可修改 (`shadcn/ui` `Calendar` 組件)。
- **飲水量輸入**: 數字輸入框，單位 `ml` (`shadcn/ui` `Input` 組件)。
- **睡眠時長輸入**: 數字輸入框，單位 `小時` (`shadcn/ui` `Input` 組件，可考慮 `Slider`)。
- **備註文本框**: 多行文本輸入 (`shadcn/ui` `Textarea` 組件)。
- **保存/取消按鈕**: (`shadcn/ui` `Button` 組件)。

## 交互流程
1. 用戶進入健康日誌頁面，預設顯示當前日期的日誌概覽。
2. 用戶可通過日期選擇器切換查看不同日期的日誌。
3. 點擊「新增日誌」按鈕，彈出表單，填寫飲水量、睡眠時長、備註等信息。
4. 點擊「保存」提交數據至 `daily_logs` 表。
5. 點擊現有日誌條目，彈出編輯表單，修改後保存。

## 建議使用的 shadcn/ui 組件
- `Card`：用於數據概覽和單個日誌條目。
- `Table`：用於日誌列表展示。
- `Calendar`：用於日期選擇。
- `Input`：用於飲水量、睡眠時長輸入。
- `Textarea`：用於備註輸入。
- `Button`：用於操作按鈕。
- `Dialog` 或 `Sheet`：用於新增/編輯日誌表單彈窗。
